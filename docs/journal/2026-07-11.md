# Journal: 2026-07-11

## Session: Notion connected + learning & finance tracking (SPEC-0002)

**Did:**
- Notion MCP connected to user's personal workspace (surajshukla5604@gmail.com; the
  Claude account being on bramerpvtltd doesn't matter — OAuth binds whichever Notion
  account is logged in). Explored the "Command Center": Journal, Tasks DB, Projects,
  Pillars, Vault (Books DB), Personal, Finance DB.
- Wrote + built [[SPEC-0002]]: one `learning` table for books AND courses (same
  lifecycle), unit-based progress (chapter/lesson), logs land as `learning.log` events;
  `transactions` table for finance with manual /finance page as EXPLICIT interim
  (automation rule intact — Epic 3 auto-ingestion remains the designed flow).
- New: /learning + /finance pages (nav updated), APIs, 5 new chat tools (logLearning,
  addLearning, queryLearning, logExpense, queryFinance), brief now coaches on
  days-since-last-reading-session + spending.
- One-time Notion import: 8 books (Steve Jobs done; Einstein + Art of Thinking Clearly
  active since 06-14) + 13 courses (Langchain active; 12 backlog). Logged user's real
  "5 chapters today" → Art of Thinking Clearly 5/99.
- Verified locally (:3005, authed): learning GET 21 items, +1 log increments 5→6,
  transaction POST saves, both pages 200. Test rows cleaned after (back to 5/99).
- Decision (user asked): Notion = one-time import + future read-only ingestor; Vivy is
  source of truth; NO two-way sync (rate limits, conflict hell, second-brain split).
  Gradual migration, keep Notion for long-form journal writing if user likes it.

**Status now:**
- SPEC-0002 code-complete, verified locally, NOT deployed. SPEC-0001 also still not
  deployed to prod (permission gate: user must run `vercel --prod --yes` themselves).
- AI Gateway unlocked (card added 2026-07-08 session); Haiku for extraction/summary,
  Sonnet for brief/chat. ~$5 credits; two crons ≈ $0.01–0.02/day.
- All work uncommitted.

**Next:**
1. USER: run `! vercel --prod --yes` — ships SPEC-0001 intelligence layer + SPEC-0002.
2. USER: click through /learning, /finance, /chat on prod; then flip remaining tested
   boxes honestly.
3. Then: Notion journal ingestor (needs user-created integration token at
   notion.so/my-integrations shared with Command Center → NOTION_API_KEY env), or
   Telegram brief delivery, or evening review cron.

**Open questions / notes:**
- Art of Thinking Clearly unitsTotal=99 (the book's real chapter count).
- Einstein + Langchain have no unitsTotal — user can set via UI later ("#ch" on add is
  only for new items; PATCH unitsTotal exists but no UI affordance yet).
- Snap node wrappers broken → `export PATH=/snap/node/current/bin:$PATH`.
- Port 3000 occupied locally; tests use 3005.

---

## Addendum 2 — charts everywhere, full finance picture, video classification, PWA (SPEC-0003)

### What changed
- **Chart-first dashboard** (`app/page.tsx` rewrite): greeting, 4 stat tiles (screen today /
  open tasks / chapters this week / spent today), collapsed brief, 2×2 grid of 14-day bar
  charts (screen time, tasks completed, chapters & lessons, daily spend), top-domains +
  learning-progress cards. Screenshot-verified via the :3007 cookie proxy.
- **Video classification**: `classifyVideos()` in `lib/ai/process-events.ts` (Haiku, one
  batched call) tags `payload.category`; `/browsing` shows a "video time by type" band;
  brief context includes the 24h split. Cron `/api/cron/process` every 4h (vercel.json).
  Verified on real data: Friends→entertainment, Coinbase breakdown→tech, Akon→music.
  (Had to reset `processed=false` for old video events that predated the classifier.)
- **Finance (SPEC-0003)**: new `positions` (assets/liabilities) + `recurring` tables pushed
  to Neon; APIs `/api/positions(+id)`, `/api/recurring(+id)`. Finance page redesigned:
  donut of month-by-category (palette validated with the dataviz six-checks script against
  the dark surface — all pass; fixed category→color map, extras fold into "everything
  else"), money-flow card (in/out/net), next-month forecast (recurring committed + variable
  at current pace), net worth section (editable asset/liability rows), recurring section
  (add/edit/pause/remove), spent/got toggle on the quick entry with income categories.
- **Learning units**: unit picker (chapter/page/section/lesson/module/hour/video/episode)
  on add + edit forms; progress logging is always "+N units" of the item's unit.
- **PWA**: `app/manifest.ts`, icons (512/192 + apple 180, headless-Chrome-rendered ember
  dot on night), proxy matcher now leaves manifest + .png public, apple/theme meta in
  layout. Login unchanged — passcode is `VIVY_PASSCODE` env.

### Status
Build clean; :3005 restarted on the new build; manifest + icons return 200 unauthenticated;
home 307→login when uncookied. Positions/recurring APIs built but not curl-tested (UI
flows exercise them). Production still runs OLD code — user must run `vercel --prod --yes`.

### Next
1. User: deploy (`! vercel --prod --yes`), then install PWA from Chrome → Add to Home screen.
2. User enters real assets/liabilities/recurring → forecast + net worth become real.
3. Net-worth history snapshots (weekly event) → trend chart, once data exists.
4. Epic 3 auto-ingestion (SMS/Gmail) to replace manual finance entry.

---

## Addendum 3 — Notion finance import, hero net worth + trend, voice input, learning grid

**Did:**
- Fetched the complete Notion **Finance Tracker** DB (17 rows) and imported it:
  10 positions + 7 recurring rules (idempotent script; College Fee ₹2.57L imported
  with `consider=false`, matching Notion's unchecked Consider). Net worth after
  import: **−₹54,911** (assets ₹7,717 − counted liabilities ₹62,628) — matches Notion.
- Schema: `positions.consider`, `positions.next_outflow`, new `networth_snapshots`
  (pushed to Neon). Snapshot upserts on every position POST/PATCH/DELETE and in the
  daily-summary cron; today's snapshot seeded.
- /finance: **net worth is now the huge centered hero** (user: only net worth huge,
  rest unchanged) with own/owe subline, excluded-amount note, and a net-worth trend
  chart (single series, dashed ₹0 line, point tooltips; one point today). Removed the
  small net-worth header stat (redundant). Forecast card now includes ₹42,000 planned
  debt payments. Liability rows show "₹X next mo" chips + "not counted" badge.
- /learning rows: flex → fixed grid tracks (`minmax(0,1fr) 9.5rem 6.5rem 1.25rem
  1.25rem`), columns finally straight; flex-wrap fallback on small screens.
- Voice input: `app/voice-button.tsx` (Web Speech API) on chat + finance note.

**Verified:** build clean; :3005 restarted on new build; GET/PATCH /api/positions
curl-verified through auth proxy (new fields present, snapshot written); screenshots
of /finance (hero + debts + recurring all real), /learning (aligned), /chat (mic).
Dictation itself needs a real mic — untested in headless.

**Status:** prod still runs old code — user must `! vercel --prod --yes`. Recurring
PATCH/DELETE untested. Notion stays source-free: this was a one-time import; the
positions table is now the live copy.

**Next:** deploy; try voice on phone (needs HTTPS = prod); recurring API tests;
net-worth trend fills in one point/day automatically.
