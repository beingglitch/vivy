# 2026-07-13

## Task structure: areas / projects / routines (SPEC-0007)

Brainstormed task organization with Suraj: his work splits into ongoing responsibilities
that never end (startup, job hunt, IoT lab), finite projects (whisper — Rust learning),
one-off tasks, and recurring activities (football). Wrote SPEC-0007 and built it.

**Decisions (his):** routines support both schedule kinds per routine — fixed days OR
×N/week target; quick-add stays one text box with optional area/project chips.

**Built:**
- Schema: `projects` + kind/status/parentId; new `routines` table (daysOfWeek int[] XOR
  timesPerWeek, projectId, active). Pushed to Neon.
- Routine done = `routine.done` event on the timeline (payload routineId + IST day),
  toggled by `POST /api/routines/[id]/done` — same rule+settle pattern as finance bills,
  no self-cloning tasks. Week = Mon–Sun IST (`lib/routines.ts`).
- APIs: /api/projects (+[id]), /api/routines (+[id], +[id]/done); tasks POST/PATCH take
  projectId.
- /tasks re-cut: quick-add with chips → AI-proposed → **Today** (due/today/doing tasks +
  routines due, with weekly progress) → **Areas & projects** (nested one level, open
  counts, "quiet Nd" stale badge at 5+ days, ✓ finish for projects, + task preselects
  chip) → Inbox → Done. `organize` drawer manages all three kinds.
- Daily brief: new `structureContext()` — routines needing attention today + quiet
  areas/projects; persona told to coach on them.
- MCP: add_task files by project name (fuzzy, never guess-creates), list_tasks grouped,
  new log_routine tool.

**Verified (curl via authproxy, local dev):** created his real structure (3 areas,
whisper project, football ×1/wk); task filed under whisper rendered in its group;
football toggle → "1 of 1 this week ✓" → un-tap removes the event; MCP round-trip
(file under "iot lab", grouped list, routine listing). Test tasks dropped. tsc clean,
prettier (--single-quote --print-width 110) applied.

**Gotcha:** local :3005 was serving a stale `next start` prod build — new API routes
404'd until it was replaced with `next dev`. If routes mysteriously 404 locally, check
`pgrep -af next`.

## Status / next
- Deploy to prod is user-side: `vercel --prod --yes` (schema already pushed to Neon, so
  prod code + db will match on deploy).
- Still to tick on SPEC-0007: browser click-through (chips, toggles, organize drawer),
  one real morning brief showing routines/quiet-areas, stale badge seen live.
- Football is a guess at ×1/week — Suraj should set the real schedule in the organize
  drawer, and add gym/other routines + file his existing tasks into the new areas.
- Older open items: voice loop mic test, age-toggle click test, night-engineer PR review.
