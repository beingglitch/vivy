# Journal: 2026-07-08

## Session: project birth — plan + repo structure

**Did:**
- Defined Vivy: personal AI assistant ecosystem (Jarvis). One spine (Event API + Postgres +
  Claude) + many automatic ingestors + proactive daily brief. Architecture in `README.md`.
- Copied the Vibe Coding OS structure from `~/Desktop/Personal2/vibe` into this repo
  (CLAUDE.md, docs/, .claude/skills).
- Filled `CLAUDE.md` Project section (name, stack, architecture + automation rules).
- Wrote full roadmap into `docs/TRACKER.md`: 5 epics (Spine, Passive Tracking, Finance,
  Meeting Pipeline, Proactive Vivy).
- Wrote [[SPEC-0001]] (spine: Event API, task pipeline, daily brief, chat) — status draft.
- Wrote [[ADR-0001]]: cloud spine on Vercel + Neon + Claude via AI SDK (user chose cloud).
- User directive captured as a hard rule: **no manual data entry** (no PDFs/CSVs by hand);
  every source gets an automatic ingestor (Gmail, bank SMS forwarder, bank API, calendar,
  browser extension, screen agents). Tracker + CLAUDE.md updated accordingly.

**Status now:**
- Docs-only repo; no code yet. Not a git repo yet (should `git init` at scaffold time).
- Active spec: SPEC-0001 (draft, ready to build).

**Next:**
- Build SPEC-0001, first tasks: scaffold Next.js app in this repo, provision Neon via
  Vercel Marketplace, wire Drizzle, then Event API + tasks schema.

**Open questions / notes:**
- Brief delivery channel: email vs Telegram bot (Telegram likely easier + free push).
- Bank connection API depends on user's country/bank — confirm before Epic 3 (SMS/Gmail
  parsing works regardless and comes first).
- Recording other people (Epic 4 hardware) needs a consent protocol.
