# Journal: 2026-07-08

## Session: project birth — plan + repo structure

**Did:**
- Defined Vivy: personal AI assistant ecosystem (Jarvis). One spine (Event API + Postgres +
  Claude) + many automatic ingestors + proactive daily brief. Architecture in `README.md`.
- Copied the Vibe Coding OS structure from `~/Desktop/Personal2/vibe` into this repo
  (CLAUDE.md, docs/, .claude/skills).
- Filled `CLAUDE.md` Project section (name, stack, architecture + automation rules).
- Wrote full roadmap into `docs/TRACKER.md`: 5 epics (Spine, Passive Tracking, Finance,
  Meeting Pipeline, Proactive Vivy).
- Wrote [[SPEC-0001]] (spine: Event API, task pipeline, daily brief, chat) — status draft.
- Wrote [[ADR-0001]]: cloud spine on Vercel + Neon + Claude via AI SDK (user chose cloud).
- User directive captured as a hard rule: **no manual data entry** (no PDFs/CSVs by hand);
  every source gets an automatic ingestor (Gmail, bank SMS forwarder, bank API, calendar,
  browser extension, screen agents). Tracker + CLAUDE.md updated accordingly.

**Status now:**
- Scaffolded Next.js 16.2.10 (App Router, TS, Tailwind 4, Turbopack) into this repo with
  create-next-app; git repo initialized by the scaffold. Dev server verified booting
  (`npm run dev`, ready in ~600ms). NOTE: port 3000 is occupied by another local service —
  dev runs on 3001.
- Toolchain quirk: `/snap/bin/node` wrappers are broken in this sandbox; use
  `export PATH=/snap/node/current/bin:$PATH` before any npm/npx command.
- Active spec: SPEC-0001, status building (scaffold task done).

**Next:**
- BLOCKED on user: `vercel login` + link project, then provision Neon Postgres via Vercel
  Marketplace and pull env vars.
- Then: install drizzle-orm/drizzle-kit + ai + @ai-sdk/react + zod, init shadcn/ui, write
  schema (`events`, `tasks`, `projects`, `briefs`, `chat_messages`, `memories`), Event API.

**Open questions / notes:**
- Brief delivery channel: email vs Telegram bot (Telegram likely easier + free push).
- Bank connection API depends on user's country/bank — confirm before Epic 3 (SMS/Gmail
  parsing works regardless and comes first).
- Recording other people (Epic 4 hardware) needs a consent protocol.

## Session: build the spine + browser analysis

**Did:**
- Linked Vercel project (`surajshuklaofficials-projects/vivy`), provisioned Neon Postgres
  via `vercel integration add neon`; env vars in `.env.local` + Vercel prod/dev.
- Built and DEPLOYED the spine (SPEC-0001 core): https://vivy-sage.vercel.app
  - `lib/db/schema.ts`: events, tasks, projects, briefs, memories, chat_messages; pushed.
  - `app/api/events`: POST (single/batch, `x-vivy-key` = VIVY_INGEST_KEY) + GET timeline.
  - Auth: `lib/auth.ts` HMAC cookie + `proxy.ts` wall; `/login` page; VIVY_PASSCODE env.
  - Tasks: API CRUD + `/tasks` UI; dashboard `/` (open tasks, today's watch/browse stats,
    latest AI summary); `/browsing` analytics (videos/searches/time-by-site, 1/7/30d).
  - AI: `/api/cron/daily-summary` (Claude via AI Gateway, `vercel.json` cron 03:30 UTC).
- Chrome MV3 extension in `extension/`: active-tab time tracking, YouTube watch events
  (title/channel/seconds via content script), search capture, batched flush every 1 min,
  options page (endpoint + key) with test button.
- Verified by running: local `next start` + prod curl — key auth 401/ok, batch insert,
  timeline GET, login, task create, auth redirect 307, authed 200. All passed.

**Status now:**
- Spine live in production. Secrets in `.env.local` (VIVY_PASSCODE printed there too).
- BLOCKED: AI Gateway returns `customer_verification_required` — user must add a credit
  card to the Vercel account to unlock free credits (or we switch to a direct
  ANTHROPIC_API_KEY). Until then `/api/cron/daily-summary` fails.
- Extension written but never loaded in a real browser — needs manual install + real test.
- Not built yet from SPEC-0001: chat with Vivy, morning task brief, AI task extraction,
  event processing loop, projects UI. Repo has uncommitted work (user hasn't asked to commit).

**Next:**
1. User: add card for AI Gateway; install extension (chrome://extensions → Load unpacked →
   `extension/`, then options → URL + VIVY_INGEST_KEY); browse a bit; check `/browsing`.
2. Then: AI task extraction + event processing loop, chat with Vivy, morning brief.

**Open questions / notes:**
- Snap node wrappers broken → always `export PATH=/snap/node/current/bin:$PATH`.
- Port 3000 occupied locally; dev on 3001, tests used 3005.

## Session: extension verified + intelligence layer built

**Did:**
- User installed the extension in real Chrome; real events flowing (video.watch with
  channel, time-by-site on /browsing). Tracker extension line flipped to tested.
- Cleaned synthetic test events from the events table.
- Built the whole intelligence layer (rest of SPEC-0001):
  - `lib/ai/` — model constant (VIVY_MODEL), Vivy persona, memory-context loader.
  - Processing loop `lib/ai/process-events.ts`: unprocessed events → text types
    (note/meeting.note/transcript/email/text) get AI task extraction (generateObject,
    aiProposed=true, sourceEventId link); analytics types just marked processed. Failures
    stay unprocessed and retry. Runs via `after()` on `POST /api/events` (instant) +
    `/api/cron/process` (manual/backlog drain; CRON_SECRET | ingest key | session).
  - Daily brief `lib/ai/daily-brief.ts` + `/api/cron/daily-brief` (cron 03:45 UTC =
    09:15 IST, drains queue first, upserts `briefs` by day). Dashboard shows brief card.
  - Chat: `/api/chat` streamText + 5 tools (queryTasks/createTask/completeTask/
    queryEvents/remember), memories injected into system prompt, messages persisted to
    chat_messages; `/chat` streaming UI (useChat, @ai-sdk/react@4). Nav link added.
  - /tasks: "Vivy proposed — approve or reject" section (approve clears aiProposed,
    reject drops); PATCH now accepts aiProposed.
- Verified what's verifiable with gateway blocked: build clean; process route 401 unauth /
  drains 29 events authed (27 marked, 2 text retried, gateway error surfaced per-event);
  note ingest 200 with extraction fired after response; daily-brief 500s on gateway
  (expected); chat 401 unauth via proxy.

**Status now:**
- Intelligence layer code-complete but NOT deployed (production deploy needs user
  go-ahead) and NOT AI-tested — still blocked on the Vercel card for AI Gateway.
- All work still uncommitted.

**Next:**
1. User: add card on Vercel (unlocks free credits) → then test: paste a note event →
   proposed tasks appear; hit /api/cron/daily-brief → brief on dashboard; chat works.
2. Deploy to prod (user go-ahead), flip tested boxes only after real runs.
3. Fast-follows: brief delivery (Telegram likely), projects/areas UI, evening review.

## Session addendum: Vivy the coach

- User directive: Vivy must COACH, not report — honest about slowness/inconsistency with
  numbers, positively framed with behavioral psychology (streaks, small wins, identity,
  tiny next steps, occasional loss framing). Example he gave: reading books regularly →
  "you're slow, you're inconsistent, but…".
- Done: VIVY_PERSONA rewritten (coaching style spec); daily brief now feeds a 7-day
  day-by-day trend (screen time, video time, tasks completed — `weeklyTrend()` in
  `lib/ai/daily-brief.ts`) and its "Note from Vivy" section is the coach moment;
  preference stored as a `memories` row (category=preference) so chat picks it up too.
  Build clean. Still undeployed + gateway-blocked like the rest.
- User also asked about Notion (second brain on surajshukla5604@gmail.com — OAuth via
  /mcp pending, account mismatch explained: log into that Notion account in browser) and
  about future clients: Android app + ESP32 hardware → both are just ingestors POSTing
  to /api/events; no architecture change needed.

## Session addendum 2: AI GATEWAY UNLOCKED — intelligence layer verified

- User added the card ($5 free credits, told me to use them wisely).
- Frugality built in: `VIVY_MODEL_FAST` (claude-haiku-4-5) now powers task extraction +
  daily browsing summary (high-volume/routine); Sonnet only for daily brief + chat.
- Local verification (one run each, port 3005): process queue → 3 text events analyzed,
  2 correct tasks proposed from the debug note (bank call due tomorrow, Rahul BOQ Friday),
  0 errors. Daily brief → real coach-style brief generated (Top 3, streak psychology,
  numbers) — quality excellent, persona working. Tracker boxes flipped accordingly.
- Cleaned stale "Install the Vivy browser extension" task (marked done — it is).
- Chat still untested end-to-end (needs browser after deploy).
- STILL PENDING: production deploy — permission system requires the user to run
  `vercel --prod --yes` themselves (I attempted twice, denied). Until then prod has no
  intelligence layer and prod crons only have daily-summary.
