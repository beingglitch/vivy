# 2026-07-12

## Personal touch + mobile navigation

**Did:**
- `settings` key-value table (pushed to Neon) + `lib/settings.ts` + `/api/settings`
  + `/settings` page (name, date of birth; gear icon in the header). Seeded:
  Suraj Shukla, 2004-03-27. Also seeded a `memories` row with his name so chat
  and the brief address him as Suraj.
- Home: greeting is now "Good morning, Suraj." (name-aware, from settings);
  net-worth hero subline shows **age 22.29** (two decimals — the clock that keeps
  the goal honest), driven by the editable DOB.
- Mobile navigation: fixed bottom tab bar (Home / Tasks / Learn / Money / Browse,
  ember active state, safe-area padding, hidden on sm+); top bar on phones is just
  the wordmark + settings gear; desktop keeps the full link row. Vivy FAB raised
  above the bar on phones; content bottom padding adjusted.

**Verified:** build clean; :3005 restarted; 390px screenshots of / (greeting
"Still up, Suraj?", age in hero, tab bar active state) and /settings (form
pre-filled). Note: scratchpad authproxy.js had vanished — recreated; restart it
when API tests 401.

**Status:** night engineer routine runs tonight 3:04 AM IST (first run). Prod
still needs the user's `! vercel --prod --yes`.

**Next:** review the first night PR; user tests voice + PWA install after deploy.

## Addendum — age got equal billing

User: the small age subline missed the point ("so I can realize how behind I am").
Both heroes (home + finance) now show NET WORTH and AGE side by side at the same
size with a divider — the confrontation is the feature. 390px screenshot-verified.

## Addendum — bills vs daily spends, and the age toggle

User: recurring and daily non-recurring spends must stay separate ("recurring
show 10000 out of 23000") — one pot hid both stories.

- `transactions.recurring_id` (FK → recurring, pushed to Neon): a payment linked
  to a recurring rule is a bill; everything else is daily.
- Entry form: "bills" chip row — one tap settles a bill (typed amount overrides
  the usual one for variable bills like electricity); settled ones show ✓.
- Finance header now: today · daily·month · bills·month (paid of total).
- Recurring section: progress bar "₹X settled of ₹Y this month" + ✓ per row.
- Money flow: "out = ₹bills + ₹daily". Today list tags bill rows.
- Double-count guards: the 14-day daily chart and the forecast's variable pace
  now exclude bill payments (bills are already counted via recurring rules).
- Home spend tile month line: "₹X daily · ₹Y bills this month"; home daily bars
  exclude bills too.

Second ask: age as a countdown, not a decimal. New `AgeDisplay` client component
on both heroes: default "DAYS TO 24 · 258", tap → "AGE · 23y 3m", tap again back;
choice persists in localStorage (`vivy-age-fmt`). (Age reads 23 now because the
DOB in settings is 2003-03-27.)

**Verified:** build clean; settled Youtube Premium ₹149 through the API → header
showed ₹149 of ₹20K bills, chip flipped to ✓, Today row tagged "bill", daily
bars unchanged — then deleted the test row. 390px screenshots of both pages.
Note: prettier has no repo config — closest match is `--single-quote
--print-width 110`.

**Next:** user deploys (`! vercel --prod --yes`); when a real bill is paid, tap
its chip so the split starts accruing.
