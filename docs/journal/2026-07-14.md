# 2026-07-14

## Goals + nightly planner (SPEC-0010) + brief markdown fix

Suraj's morning asks, in one arc: set real goals ("liquid 1 million in 2 years,
health goals, reading goals"), have the AI optimize his day from them, plan tomorrow
by talking to Vivy at night (calendar-aware), simple health logging by voice ("ate
lunch", "going to sleep" — asked what always-on listening would cost), and the brief
was showing literal `**`.

**His decisions (AskUserQuestion):** voice = MCP tools + one-tap mic, NOT always-on
(after the honest problems list: browsers can't background-mic, Android foreground
service = battery + permanent notification + OEM kills, wake word needs on-device
engine); calendar = **full Google OAuth** (read AND write); build order = goals +
planner first, health/voice capture next (SPEC-0011).

**Fixed first:** brief rendered as raw markdown in a `<pre>` → `app/brief-content.tsx`,
a ~70-line renderer for exactly what the model emits (**bold**, *italic*, bullets,
`**Section**` headers → ember uppercase). Verified via rendered HTML: zero literal
asterisks. Commit 68e93c1.

**Built (SPEC-0010):**
- Schema: `goals` (metric/target/startValue/deadline — startValue auto-baselined at
  creation, progress NEVER typed in), `connections` (OAuth tokens, one row per
  provider), `plans` (day-unique like briefs; intent + markdown + jsonb blocks +
  calendarEventIds). Pushed to Neon.
- `lib/goals.ts`: metric registry (networth from positions, books_finished,
  learning_units_week from learning.log events); pace = progress-fraction vs
  time-fraction; "behind; needs +₹X/month" lines; goalsContext() for AI prompts.
- `lib/google.ts`: OAuth + Calendar over plain fetch (googleapis SDK skipped —
  ~10MB for four calls). Auth URL, code exchange (email from id_token), token
  refresh, listEvents(day), createBlocks (titled `[vivy] …`), deleteBlocks —
  replan replaces exactly our blocks, never his.
- `lib/ai/planner.ts`: planContext (calendar live + structureContext + goals +
  due tasks), generateObject plan {content markdown, blocks[]}, rules: never move
  fixed events, meal gaps, 08:00–23:30, 45–120min focus blocks, serve behind-pace
  goals. todaysPlanContext() feeds the morning brief ("hold me to it").
- APIs: /api/goals(+[id]), /api/google/auth|callback|disconnect (redirect URI from
  request origin — same code local+prod), /api/plan (GET ctx/POST generate),
  /api/plan/calendar (delete old [vivy] ids → create → save ids).
- UI: /goals (pace bars with a "where you should be" tick), home rail Goals card,
  /plan (fixed-of-tomorrow → intent textarea → plan → "Put blocks on my calendar"),
  settings Google card, nav "Plan". Evening nudge now links /plan.

**Verified locally (authproxy curls + headless screenshots):** test money goal
auto-baselined at real net worth −₹67,197, target ₹10L by 2028 → "behind; needs
+₹44k/month" (math checks: 1,067,197/24 ≈ 44.5k). Plan generation end-to-end with
real model: 4 sane blocks, meal gaps, football coach note tied to real 0/1-this-week
data. Test goal + test plan deleted after. tsc clean.

**Also:** answered "Tauri instead of Kotlin?" — no: the Android app is a
UsageStatsManager collector, which needs Kotlin either way; Tauri would add a Rust
layer + webview for zero benefit. Table stays in chat; no ADR (no decision changed).

## Status / next
- **USER STEPS, in order:**
  1. Vercel → Root Directory = `apps/web` (his deploy screenshot this morning =
     exactly this failure), + VAPID env vars, then deploy.
  2. Google Cloud console: project → enable Calendar API → OAuth consent (External,
     test mode, himself as test user) → OAuth client (Web), redirect URIs
     `http://localhost:3005/api/google/callback` + prod equivalent →
     GOOGLE_CLIENT_ID/SECRET into apps/web/.env.local + Vercel → then /settings
     "Connect Google Calendar".
  3. Set his real goals on /goals; tonight at 21:30 the evening nudge opens /plan.
  4. Still pending from SPEC-0009: run apps/desktop/install.sh; sideload APK.
- Next spec: **SPEC-0011 health/voice capture** — meal.logged/sleep events, MCP
  log_meal/log_sleep tools (Claude app voice mode = zero-build voice today),
  PWA mic button, Android quick tile; sleep inference from phone.usage overnight
  gap as the no-manual-entry path.
- Open verify items on SPEC-0010: live calendar read/write after he supplies OAuth
  creds; first real nightly plan → morning brief carrying it.

## Addendum: phone landed + SPEC-0011 health/voice capture

Suraj sideloaded the APK — **first real phone.usage events in the timeline**
(2026-07-14: Stremio 259min, YouTube 171min, Chrome 55min…). His screenshot also
showed the settings screen was unreadable ("I CAN'T SEE WHAT IS THAT"): hints vanish
when filled, key masked to dots, no labels. He then said build SPEC-0011 now.

**Built:**
- `lib/health.ts`: meal.logged / sleep.start / sleep.wake events (IST day in payload,
  processed=true); healthToday (distinct meals, asleep?, last night's hours from the
  wake−sleep pair, capped 16h); healthContext() — returns '' if health events have
  never been used, so the brief doesn't nag about an unused feature. Wired into
  daily-brief context and eveningReview facts.
- `/api/voice-log`: one endpoint for every speech surface — free text in, Haiku
  (VIVY_MODEL_FAST) classifies meal/sleep/wake/note (meal guessed from words or
  Indian time-of-day), keyword fallback if the model errors, `note` event if
  unclassifiable. Auth = session cookie OR x-vivy-key (proxy exception added).
  Returns a short `say` string for the caller to speak/toast.
- Chat tools logMeal/logSleep (chat mic → "I ate lunch" works); MCP tools
  log_meal/log_sleep (Claude app voice mode path).
- Android: MainActivity rebuilt for legibility (bold labels, un-masked key,
  ScrollView, intro line); VoiceLogActivity (RecognizerIntent → POST voice-log →
  toast Vivy's reply); VivyTileService — "Log to Vivy" quick-settings tile
  (PendingIntent variant for API 34+). assembleDebug green, 2.28MB.

**Verified:** voice-log with key → `{"logged":"meal","meal":"lunch"}`; via session →
sleep.start; no auth → 401. Events inspected in Neon then deleted (source='test').
tsc clean. Chat spoken round-trip and on-phone tile are Suraj's to try.

**Next:** tag (v0.2.0) so he can install the fixed APK from /settings; sleep
inference from phone.usage gaps once a few days of data exist; health goal metrics
into the SPEC-0010 registry.
