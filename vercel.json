{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "crons": [
    {
      "path": "/api/push/run",
      "comment": "06:00 IST. Vercel schedules in UTC and has no timezone setting, so this is 00:30 UTC. Change it twice if you ever move somewhere with daylight saving; India has none.",
      "schedule": "30 0 * * *"
    }
  ]
}
