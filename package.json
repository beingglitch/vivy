{
  "name": "vivy-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "services/*"
  ],
  "scripts": {
    "dev": "npm run dev -w vivy",
    "build": "npm run build -w vivy",
    "lint": "npm run lint -w vivy"
  }
}
